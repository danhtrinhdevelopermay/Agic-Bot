services:
  - type: web
    name: facebook-messenger-bot
    env: node
    plan: free
    buildCommand: npm ci && node build-render.js
    startCommand: npm start
    healthCheckPath: /api/test
    envVars:
      - key: NODE_ENV
        value: production